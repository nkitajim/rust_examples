# 3. 上級（非同期・スレッド・パフォーマンス）
```
- 非同期 HTTP クライアント
tokio と reqwest を使って複数 URL の内容を非同期取得
async/await とタスク並列の練習。

- 並列マルチスレッド計算
std::thread で並列にフィボナッチ数列や素数計算
Arc + Mutex を使って共有状態を安全に扱う。

- 簡易チャットサーバー
tokio + TCP ソケットで複数クライアントに対応
非同期 I/O とメッセージパイプを体験。
```
