# 中級（所有権・ライフタイム・コレクション）
```
- 簡易リンクリストの実装
enum List { Cons(i32, Box<List>), Nil } を使って、
要素の追加
合計値計算
リストの表示

再帰的データ構造と Box を理解する練習。

- ファイルのバックアップユーティリティ
指定ディレクトリ内のファイルを別ディレクトリにコピー
std::fs::read_dir, std::fs::copy, Path を使う
Result と ? を使ったエラー処理で Rust らしい安全性を体験。

- 所有権を意識したテキスト操作
ファイルを読み込んで行ごとに加工（trim、置換など）して別ファイルに書き出す
借用・所有権の管理を自然に体験できる。
```
